@***************************************************@
@*Esta diretiva define que esse arquivo é um layout*@
@***************************************************@
@inherits LayoutComponentBase

@*Container principal que envolve a estrutura visual da página*@
<div class="page">
    <div class="sidebar">

        @*-*-*-*-*-*-*-*-*-*-*-*-*-*--*-*-*-*-*-*-*-*-*-*@
        @*  Os layouts podem conter outros componentes  *@
        @*-*-*-*-*-*-*-*-*-*-*-*-*-*--*-*-*-*-*-*-*-*-*-*@

        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @*==========================================================================================*@
            @* Coração do layout *@
            @* É uma diretiva do blazor, utilizada em layouts razor *@
            @* Ela representa o conteúdo da página filha que está sendo renderizada dentro desse layout *@
            @*==========================================================================================*@
            @Body
        </article>
    </main>
</div>

@***********************************************************************@
@* Container de erro do Blazor , usado para exibir um erro não tratado *@
@***********************************************************************@
<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>
